# Copilot Instructions

## General Guidelines
- Use `IActionResult` return type for controller actions.
- Return `Json(result)` in controller actions.
- Implement try/catch blocks for error handling in controller actions.
- Use appropriate HTTP annotations for actions.